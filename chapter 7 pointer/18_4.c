#include<stdio.h>
int main(){
    int a[10]={10,0,9,8,11,25,-10,5,-2,20};
    int sort(int*);
    short(a);
    for (int i=0;i<10;i++)
    printf("\t %d",a[i]);
}
int sort(int*a){
    int i,j;
    for (i=0,i<10;i++){
        for (i= 0,i<10;i++)
    }
}
//arrange the array in accending order or swap the value of the array 