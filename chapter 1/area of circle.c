#include<stdio.h>
int main(){
     int radius;
    printf("enter the value of radius:");
    scanf("%d",&radius);
    int area;
    float pi=3.14;
    area=pi*radius*radius;
printf("area is:%d",area);
return 0;
}